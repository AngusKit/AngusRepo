<!--
  MACHINE-READABLE METADATA
  document_type: api_development_specification
  project: AngusRepo
  version: 2.0.0
  language: zh-CN
  framework: Spring Boot 3 + DDD + JPA
  base_library: AngusInfra (cloud.xcan.angus)
  last_updated: 2026-03
  coverage:
    - project_structure
    - naming_conventions
    - layer_responsibilities
    - restful_url_design
    - http_methods
    - response_format
    - parameter_validation
    - exception_handling
    - error_codes
    - security
    - logging
    - database_naming
    - transaction_management
    - api_documentation
    - prohibited_actions
    - code_templates
    - ai_parsability
-->

# AngusRepo 后端接口开发规范（完整版 v2.0）

> **本文档基于 AngusRepo 项目的实际代码（Controller、Service、Repository、DTO/VO、Entity）提炼而成，所有示例均来自真实代码，可作为 AI 代码生成和代码审查的执行标准。**

---

## 📑 目录

1. [项目结构和模块划分](#1-项目结构和模块划分)
2. [类 / 方法 / URL 命名约定](#2-类--方法--url-命名约定)
3. [分层职责](#3-分层职责)
4. [RESTful URL 设计规则](#4-restful-url-设计规则)
5. [HTTP 方法与操作的对应关系](#5-http-方法与操作的对应关系)
6. [统一响应格式](#6-统一响应格式)
7. [参数校验](#7-参数校验)
8. [异常分类与全局处理](#8-异常分类与全局处理)
9. [错误码体系](#9-错误码体系)
10. [安全要求](#10-安全要求)
11. [日志级别与内容要求](#11-日志级别与内容要求)
12. [数据库 / 表 / 字段命名规范](#12-数据库--表--字段命名规范)
13. [事务管理规则](#13-事务管理规则)
14. [API 文档（Swagger/OpenAPI 注解要求）](#14-api-文档swaggeropenapi-注解要求)
15. [禁止事项清单](#15-禁止事项清单)
16. [标准代码样板](#16-标准代码样板)
17. [规范文档结构说明（AI 可解析）](#17-规范文档结构说明ai-可解析)

---

## 1. 项目结构和模块划分

### 1.1 顶层模块结构

```
AngusRepo/
├── service/
│   ├── api/          # 公共 API 层 —— 对外暴露的 DTO、VO、Remote 接口
│   ├── core/         # 核心业务层 —— DDD 四层架构实现
│   ├── boot/         # 启动模块 —— Spring Boot 主入口、配置
│   ├── conf/         # 配置文件
│   └── extension/    # 扩展插件（如短信、云服务）
├── web/              # 前端资源
├── assembly/         # 打包组装
└── docs/             # 规范文档
```

### 1.2 DDD 四层架构（core 模块）

```
service/core/src/main/java/cloud/xcan/angus/core/gm/
├── interfaces/                  # 接口层
│   └── {module}/
│       ├── {Module}Rest.java           # REST 控制器
│       └── facade/
│           ├── {Module}Facade.java     # 门面接口
│           ├── dto/                    # 数据传输对象
│           ├── vo/                     # 视图对象
│           └── internal/
│               ├── {Module}FacadeImpl.java   # 门面实现
│               └── assembler/
│                   └── {Module}Assembler.java
├── application/                 # 应用层
│   ├── cmd/{module}/
│   │   ├── {Module}Cmd.java            # 命令接口（写操作）
│   │   └── impl/{Module}CmdImpl.java
│   └── query/{module}/
│       ├── {Module}Query.java          # 查询接口（读操作）
│       └── impl/{Module}QueryImpl.java
├── domain/                      # 领域层
│   └── {module}/
│       ├── {Module}.java               # 领域实体
│       ├── {Module}Repo.java           # 仓储接口
│       └── enums/
└── infra/                       # 基础设施层
    └── persistence/
        ├── mysql/{module}/{Module}RepoMysql.java
        └── postgres/{module}/{Module}RepoPostgres.java
```

### 1.3 api 模块（公共接口）

```
service/api/src/main/java/cloud/xcan/angus/
├── api/commonlink/    # 跨模块公共实体、仓储接口
│   ├── user/User.java, UserRepo.java
│   ├── department/, group/, role/, quota/ ...
├── api/gm/            # 对外 Feign 远程接口
│   ├── user/UserRemote.java
│   └── email/EmailRemote.java
└── api/manager/       # 领域服务 Manager（跨模块调用）
    └── impl/
```

### 1.4 依赖规则

| 方向 | 允许 ✅ | 禁止 ❌ |
|------|--------|--------|
| 接口层 | → 应用层、领域层 | → 基础设施层 |
| 应用层 | → 领域层 | → 基础设施层、接口层 |
| 领域层 | 仅依赖自身 | → 任何其他层 |
| 基础设施层 | → 领域层（实现接口） | → 应用层、接口层 |

---

## 2. 类 / 方法 / URL 命名约定

### 2.1 类名规范

| 类型 | 命名规则 | 示例 |
|------|---------|------|
| REST 控制器 | `{Module}Rest` | `DepartmentRest`, `UserRest` |
| 内部 API | `{Module}InnerRest` | `EmailInnerRest` |
| 门面接口 | `{Module}Facade` | `DepartmentFacade` |
| 门面实现 | `{Module}FacadeImpl` | `DepartmentFacadeImpl` |
| 数据组装器 | `{Module}Assembler` | `DepartmentAssembler` |
| 命令接口 | `{Module}Cmd` | `DepartmentCmd` |
| 命令实现 | `{Module}CmdImpl` | `DepartmentCmdImpl` |
| 查询接口 | `{Module}Query` | `DepartmentQuery` |
| 查询实现 | `{Module}QueryImpl` | `DepartmentQueryImpl` |
| 创建 DTO | `{Module}CreateDto` | `DepartmentCreateDto` |
| 更新 DTO | `{Module}UpdateDto` | `DepartmentUpdateDto` |
| 查询 DTO | `{Module}FindDto` | `DepartmentFindDto` |
| 详情 VO | `{Module}DetailVo` | `DepartmentDetailVo` |
| 列表 VO | `{Module}ListVo` | `DepartmentListVo` |
| 领域实体 | `{Module}` | `Department`, `User` |
| 仓储接口 | `{Module}Repo` | `DepartmentRepo` |
| MySQL 实现 | `{Module}RepoMysql` | `DepartmentRepoMysql` |
| PostgreSQL 实现 | `{Module}RepoPostgres` | `DepartmentRepoPostgres` |
| Remote 接口 | `{Module}Remote` | `UserRemote` |
| Inner Remote | `{Module}InnerRemote` | `UserInnerRemote` |

### 2.2 方法名规范

**REST 方法名（Controller 层）：**

| 操作 | 方法名 | 示例 |
|------|-------|------|
| 创建 | `create` | `create(dto)` |
| 完整更新 | `update` | `update(id, dto)` |
| 部分更新 | `patch` 或 `updateXxx` | `patch(id, dto)`, `updateStatus(id, dto)` |
| 删除单个 | `delete` | `delete(id)` |
| 批量删除 | `deleteBatch` | `deleteBatch(dto)` |
| 获取详情 | `getDetail` | `getDetail(id)` |
| 获取列表 | `list` | `list(dto)` |
| 获取统计 | `getStats` | `getStats()` |
| 获取树 | `getTree` | `getTree(...)` |

**Facade / Query / Cmd 方法名：**

| 操作 | 方法名 |
|------|-------|
| 创建 | `create` |
| 更新 | `update` |
| 删除 | `delete` |
| 查询单个（含校验） | `findByXxxAndCheck` / `getXxx` |
| 查询单个（不校验） | `findByXxx` |
| 查询列表 | `list` / `find` |
| 批量操作 | `batchXxx` |
| 统计 | `getStatistics` / `count` |

### 2.3 包名规范

- 全小写，多单词用 `.` 分隔
- 业务模块名为单数名词：`user`, `department`, `group`, `role`, `quota`
- 子包规范：`dto`, `vo`, `internal`, `assembler`, `enums`, `impl`

### 2.4 常量使用规范

**✅ DO：使用 AngusInfra 提供的 `BizConstant` 常量**

```java
import static cloud.xcan.angus.spec.experimental.BizConstant.MAX_NAME_LENGTH;      // 100
import static cloud.xcan.angus.spec.experimental.BizConstant.MAX_NAME_LENGTH_X2;   // 200
import static cloud.xcan.angus.spec.experimental.BizConstant.MAX_NAME_LENGTH_X4;   // 400
import static cloud.xcan.angus.spec.experimental.BizConstant.MAX_CODE_LENGTH;      // 80
import static cloud.xcan.angus.spec.experimental.BizConstant.MAX_DESC_LENGTH;      // 200
import static cloud.xcan.angus.spec.experimental.BizConstant.MAX_URL_LENGTH;       // 200
import static cloud.xcan.angus.spec.experimental.BizConstant.MAX_BATCH_SIZE;       // 200

@Size(max = MAX_NAME_LENGTH)
private String name;
```

**❌ DON'T：硬编码魔法数字**

```java
// 错误：直接写数字
@Size(max = 100)
private String name;
```

---

## 3. 分层职责

### 3.1 接口层（Interfaces Layer）

**职责（DO）：**
- 接收和解析 HTTP 请求参数
- 触发 JSR-303 参数校验（`@Valid`）
- **只能调用门面层（Facade）方法**，不得直接调用 Cmd/Query 服务、Manager 或 Repository
- 包装统一响应（`ApiLocaleResult`）
- 提供 Swagger/OpenAPI 注解

**禁止（DON'T）：**
- ❌ 包含任何业务逻辑
- ❌ **直接调用应用层（Cmd / Query）、基础设施层（Repo）或任何 Manager**
- ❌ 直接操作领域对象
- ❌ 处理事务

**关键注解：**
```java
@Tag(name = "...")
@Validated
@RestController
@RequestMapping("/api/v1/{resources}")
```

### 3.2 Facade 层（接口层内部）

**职责（DO）：**
- 协调 Cmd/Query 服务完成业务用例
- 将 DTO 转换为领域对象（via Assembler）
- 将领域对象转换为 VO（via Assembler）
- 批量填充关联数据（避免 N+1）
- 使用 `@NameJoin` 注解自动填充关联名称
- 构建分页结果（`buildVoPageResult`）

**禁止（DON'T）：**
- ❌ **直接调用基础设施层（Repo 类），必须通过应用层（Cmd/Query）访问数据**
- ❌ 添加 `@Transactional`
- ❌ 包含核心业务规则
- ❌ 直接操作数据库（含 EntityManager）

> **规则：Facade 中调用的 Query 和 Cmd 方法，若从门面层发起，必须在对应的 Cmd/Query 方法中包含 `BizTemplate` 模板；若为非门面层调用（如内部 Manager 调用），则不需要。**

### 3.3 应用层（Application Layer）

**Cmd 服务职责（DO）：**
- 执行写操作（创建、更新、删除）
- 管理事务（`@Transactional(rollbackFor = Exception.class)`）
- 使用 `BizTemplate` 包装业务流程
- 参数校验（`checkParams()`）+ 业务处理（`process()`）
- 记录操作日志

**Query 服务职责（DO）：**
- 执行读操作（单个查询、列表查询、统计）
- 类级别标注 `@Transactional(readOnly = true)`
- 使用 `BizTemplate` 包装
- 批量填充关联数据（`setXxxUser` 等方法）
- 支持全文搜索（fullTextSearch 切换 SearchRepo）
- **特例：统计类方法（如 `getStatistics()`）可直接返回统计 VO（`XxxStatisticsVo`）**，此时 Query 接口可引用接口层 VO 包中的统计 VO 类型

**禁止（DON'T）：**
- ❌ Cmd 中执行纯查询（应在 Query 中）
- ❌ Query 中执行数据写入
- ❌ 绕过领域对象直接操作 SQL
- ❌ **在应用层（Cmd/Query）中使用普通 DTO（XxxCreateDto / XxxUpdateDto / XxxFindDto 等），仅统计 VO 特例除外**
- ❌ **在基础设施层（RepoMysql / RepoPostgres）中引用任何 DTO 或 VO**

> **DTO/VO 层级约束：**
> - DTO（CreateDto / UpdateDto / FindDto 等）和 VO（DetailVo / ListVo 等）**只能出现在接口层**（`interfaces/` 包下）
> - 统计 VO（`XxxStatisticsVo`、`XxxUsageVo`）作为特例，**允许在应用层 Query 接口中作为方法返回类型**（见 `QuotaQuery.getStatistics()` → `QuotaStatisticsVo`）
> - 领域层和基础设施层不得出现任何 DTO/VO 类

### 3.4 领域层（Domain Layer）

**职责（DO）：**
- 定义领域实体（含业务行为方法）
- 定义仓储接口（`XxxRepo extends BaseRepository`）
- 定义枚举类型
- 包含业务规则

**禁止（DON'T）：**
- ❌ 依赖其他层（保持纯净）
- ❌ 包含 HTTP/Spring 相关注解（除 JPA 外）
- ❌ 直接注入 Spring Bean

### 3.5 基础设施层（Infrastructure Layer）

**职责（DO）：**
- 实现领域层定义的仓储接口
- MySQL 实现：`{Module}RepoMysql extends {Module}Repo`
- PostgreSQL 实现：`{Module}RepoPostgres extends {Module}Repo`
- 使用 `@Repository("xxxRepo")` 注解（同名以支持数据库切换）

**禁止（DON'T）：**
- ❌ 包含业务逻辑
- ❌ 依赖应用层或接口层

---

## 4. RESTful URL 设计规则

### 4.1 基本规则

| 规则 | 正确 ✅ | 错误 ❌ |
|------|--------|--------|
| 使用复数名词 | `/api/v1/departments` | `/api/v1/department` |
| 版本号在路径中 | `/api/v1/users` | `/api/users` |
| 全小写加连字符 | `/api/v1/email-templates` | `/api/v1/emailTemplates` |
| 子资源用嵌套路径 | `/api/v1/departments/{id}/users` | `/api/v1/deptUsers?deptId=` |
| 不在 URL 中用动词 | `/api/v1/roles/{id}/permissions` | `/api/v1/roles/getPermissions/{id}` |
| 特定操作用路径后缀 | `/api/v1/email/{id}/retry` | `/api/v1/retryEmail/{id}` |

### 4.2 URL 模式

```
# 标准 CRUD
POST    /api/v1/{resources}                    # 创建
PUT     /api/v1/{resources}/{id}               # 完整更新
PATCH   /api/v1/{resources}/{id}/{sub-action}  # 部分更新 / 状态变更
DELETE  /api/v1/{resources}/{id}               # 删除
GET     /api/v1/{resources}/{id}               # 详情
GET     /api/v1/{resources}                    # 列表（分页）

# 子资源
GET     /api/v1/{resources}/{id}/{sub-resources}
POST    /api/v1/{resources}/{id}/{sub-resources}
DELETE  /api/v1/{resources}/{id}/{sub-resources}/{subId}

# 统计/聚合
GET     /api/v1/{resources}/stats
GET     /api/v1/{resources}/{id}/stats

# 特定操作（用 PATCH 或 POST）
PATCH   /api/v1/{resources}/{id}/status        # 状态变更
PATCH   /api/v1/{resources}/{id}/manager       # 设置负责人
POST    /api/v1/{resources}/{id}/retry         # 重试操作
```

### 4.3 内部 API（服务间调用）

```
# 内部 API 使用 /innerapi 前缀
POST   /innerapi/v1/{resources}
GET    /innerapi/v1/{resources}/{id}
```

### 4.4 实际项目 URL 示例

```
# 部门模块
POST    /api/v1/departments
PUT     /api/v1/departments/{id}
PATCH   /api/v1/departments/{id}/status
PATCH   /api/v1/departments/{id}/manager
DELETE  /api/v1/departments/{id}
GET     /api/v1/departments/{id}
GET     /api/v1/departments
GET     /api/v1/departments/stats
GET     /api/v1/departments/tree
GET     /api/v1/departments/{id}/path
GET     /api/v1/departments/{id}/children

# 角色权限
PUT     /api/v1/roles/{id}/permissions
GET     /api/v1/roles/{id}/permissions
GET     /api/v1/roles/{id}/users
GET     /api/v1/roles/{id}/departments
GET     /api/v1/roles/{id}/authorizable-menus
```

---

## 5. HTTP 方法与操作的对应关系

### 5.1 标准对照表

| HTTP 方法 | 操作语义 | HTTP 状态码 | 返回值 | 幂等性 |
|-----------|---------|-----------|-------|-------|
| `POST` | 创建资源 | `201 Created` | 创建后的资源 VO | 否 |
| `PUT` | 完整替换（全字段更新） | `200 OK` | 更新后的资源 VO | 是 |
| `PATCH` | 部分更新（状态/字段） | `200 OK` | 更新后的资源 VO | 是 |
| `DELETE` | 删除资源 | `204 No Content` | 无 | 是 |
| `GET` | 查询（无副作用） | `200 OK` | 资源 VO / 分页结果 | 是 |

### 5.2 特殊操作的 HTTP 方法选择

| 操作 | 方法 | URL 示例 |
|------|------|---------|
| 状态变更（启用/禁用） | `PATCH` | `PATCH /api/v1/roles/{id}/status` |
| 设置负责人/管理者 | `PATCH` | `PATCH /api/v1/departments/{id}/manager` |
| 发送邮件（有副作用） | `POST` | `POST /api/v1/email/send` |
| 重试（触发动作） | `POST` | `POST /api/v1/email/{id}/retry` |
| 取消 | `POST` | `POST /api/v1/email/{id}/cancel` |
| 刷新（数据同步） | `POST` | `POST /api/v1/services/refresh` |
| 批量删除 | `DELETE` | `DELETE /api/v1/users` + 请求体 |
| 设置默认 | `PUT` | `PUT /api/v1/roles/{id}/default` |

### 5.3 代码示例

```java
// ✅ 正确：创建用 POST + 201
@ResponseStatus(HttpStatus.CREATED)
@PostMapping
public ApiLocaleResult<UserDetailVo> create(@Valid @RequestBody UserCreateDto dto) {
  return ApiLocaleResult.success(userFacade.create(dto));
}

// ✅ 正确：完整更新用 PUT + 200
@ResponseStatus(HttpStatus.OK)
@PutMapping("/{id}")
public ApiLocaleResult<UserDetailVo> update(@PathVariable Long id,
    @Valid @RequestBody UserUpdateDto dto) {
  return ApiLocaleResult.success(userFacade.update(id, dto));
}

// ✅ 正确：状态更新用 PATCH + 200
@ResponseStatus(HttpStatus.OK)
@PatchMapping("/{id}/status")
public ApiLocaleResult<DepartmentDetailVo> updateStatus(@PathVariable Long id,
    @Valid @RequestBody EnabledStatusUpdateDto dto) {
  return ApiLocaleResult.success(departmentFacade.updateStatus(id, dto));
}

// ✅ 正确：删除用 DELETE + 204，无返回值
@ResponseStatus(HttpStatus.NO_CONTENT)
@DeleteMapping("/{id}")
public void delete(@PathVariable Long id) {
  departmentFacade.delete(id);
}

// ✅ 正确：列表查询用 GET + 200，@ParameterObject
@ResponseStatus(HttpStatus.OK)
@GetMapping
public ApiLocaleResult<PageResult<DepartmentDetailVo>> list(
    @Valid @ParameterObject DepartmentFindDto dto) {
  return ApiLocaleResult.success(departmentFacade.list(dto));
}
```

---

## 6. 统一响应格式

### 6.1 响应包装类

所有接口使用 `cloud.xcan.angus.remote.ApiLocaleResult<T>` 作为响应包装类（基于 AngusInfra）。

```java
// ApiResult 基础结构（来自 AngusInfra）
public class ApiResult<T> {
  private String code;        // 成功："S"，失败：错误码（如 "B_PROTOCOL_001"）
  private String message;     // 本地化消息
  private T data;             // 业务数据（可为 null）
  private LocalDateTime datetime; // 响应时间戳
  private Map<String, Object> ext; // 扩展字段（如 eKey、traceId）
}
```

### 6.2 成功响应格式

```json
{
  "code": "S",
  "message": "操作成功",
  "data": {
    "id": 123,
    "name": "研发部",
    "status": "ENABLED"
  },
  "datetime": "2026-03-16T08:27:26"
}
```

### 6.3 分页响应格式

```json
{
  "code": "S",
  "message": "操作成功",
  "data": {
    "total": 100,
    "list": [
      { "id": 1, "name": "研发部" },
      { "id": 2, "name": "运营部" }
    ]
  },
  "datetime": "2026-03-16T08:27:26"
}
```

### 6.4 错误响应格式

```json
{
  "code": "B_PROTOCOL_001",
  "message": "资源「用户」[ID: 123] 不存在",
  "data": null,
  "datetime": "2026-03-16T08:27:26",
  "ext": {
    "eKey": "RESOURCE_NOT_FOUND"
  }
}
```

### 6.5 空响应（DELETE 204）

DELETE 操作返回 `void`，HTTP 状态码 204，无响应体。

### 6.6 Controller 中的构建方式

```java
// 成功（带数据）
return ApiLocaleResult.success(facade.doSomething());

// 成功（带自定义消息）
return ApiLocaleResult.success("操作成功", facade.doSomething());

// 成功（无数据）
return ApiLocaleResult.success();

// 分页（data 为 PageResult<T>）
return ApiLocaleResult.success(facade.list(dto));
```

---

## 7. 参数校验

### 7.1 JSR-303 注解校验（DTO 层）

```java
@Getter
@Setter
@Schema(description = "创建部门请求参数")
public class DepartmentCreateDto implements Serializable {

  @NotBlank
  @Size(max = MAX_NAME_LENGTH)
  @Schema(description = "部门名称", requiredMode = RequiredMode.REQUIRED)
  private String name;

  @NotBlank
  @Size(max = MAX_CODE_LENGTH)
  @Schema(description = "部门编码", requiredMode = RequiredMode.REQUIRED)
  private String code;

  @Size(max = MAX_DESC_LENGTH)
  @Schema(description = "部门描述")
  private String description;

  @NotNull
  @Schema(description = "上级部门ID，根部门填0")
  private Long parentId;

  @Min(value = 0)
  @Schema(description = "排序权重")
  private Integer sortOrder;

  @Valid
  @Size(max = MAX_BATCH_SIZE)
  @Schema(description = "成员ID列表")
  private List<@NotNull Long> memberIds;
}
```

**常用注解说明：**

| 注解 | 适用类型 | 说明 |
|------|---------|------|
| `@NotNull` | 任意 | 不能为 null |
| `@NotBlank` | String | 不能为 null、空串、纯空白 |
| `@NotEmpty` | Collection/String | 不能为 null 或空 |
| `@Size(min,max)` | String/Collection | 长度/大小范围 |
| `@Min(value)` | 数字 | 最小值 |
| `@Max(value)` | 数字 | 最大值 |
| `@Email` | String | 邮箱格式 |
| `@Pattern(regexp)` | String | 正则匹配 |
| `@Valid` | 对象/集合 | 级联校验 |

### 7.2 分页查询 DTO 规范

分页查询 DTO 必须继承 `PageQuery`，并重写 `getDefaultOrderBy()` 指定默认排序字段。

```java
@Data
@EqualsAndHashCode(callSuper = true)
@Schema(description = "查询部门请求参数")
public class DepartmentFindDto extends PageQuery {

  @Schema(description = "部门ID")
  private Long id;

  @Schema(description = "部门名称（模糊搜索）")
  private String name;

  @Schema(description = "状态筛选")
  private EnabledStatus status;

  @Override
  public String getDefaultOrderBy() {
    return "createdDate";   // 必须重写，指定默认排序字段
  }
}
```

**`PageQuery` 标准字段（AngusInfra）：**

| 字段 | 类型 | 默认值 | 说明 |
|------|------|-------|------|
| `pageNo` | Integer | 1 | 页码（从1开始） |
| `pageSize` | Integer | 10 | 每页数量 |
| `orderBy` | String | `getDefaultOrderBy()` | 排序字段 |
| `orderSort` | OrderSort | DESC | 排序方向 |
| `fullTextSearch` | boolean | false | 是否全文搜索 |
| `filters` | List | null | 动态筛选条件 |

### 7.2.1 分页查询完整示例（端到端）

以下展示分页查询从请求到响应的完整数据流：

```
GET /api/v1/departments?pageNo=1&pageSize=10&orderBy=createdDate&orderSort=DESC&name=研发
          │
          ▼
┌─────────────────────────────────────────────────┐
│  1. REST 控制器 接收请求                          │
└─────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────┐
│  2. Facade 构建查询条件、调用 Query、构建 VO 分页  │
└─────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────┐
│  3. Query 执行分页查询（DB or 全文搜索）           │
└─────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────┐
│  4. 返回 ApiLocaleResult<PageResult<XxxDetailVo>> │
└─────────────────────────────────────────────────┘
```

**第一步：REST 控制器**

```java
// 接口层 - 分页查询入口
@Operation(operationId = "getDepartmentList", summary = "获取部门列表",
    description = "支持分页、排序、模糊搜索和状态筛选")
@ApiResponses(value = {
    @ApiResponse(responseCode = "200", description = "部门列表获取成功")
})
@ResponseStatus(HttpStatus.OK)
@GetMapping
public ApiLocaleResult<PageResult<DepartmentDetailVo>> list(
    @Valid @ParameterObject DepartmentFindDto dto) {  // ① @ParameterObject 将 DTO 展开为 Query Params
  return ApiLocaleResult.success(departmentFacade.list(dto));
}
```

**第二步：Facade 门面实现**

```java
// 门面层 - 组装查询、调用 Query、批量填充关联数据
@NameJoin
@Override
public PageResult<DepartmentDetailVo> list(DepartmentFindDto dto) {
  // ① FindDto → GenericSpecification（查询条件）
  GenericSpecification<Department> spec = DepartmentAssembler.getSpecification(dto);

  // ② dto.tranPage() → PageRequest（分页+排序参数）
  Page<Department> page = departmentQuery.find(
      spec,
      dto.tranPage(),                                     // 自动处理 pageNo/pageSize/orderBy/orderSort
      dto.fullTextSearch,                                 // 是否启用全文搜索
      getMatchSearchFields(DepartmentFindDto.class));     // 全文搜索匹配字段

  // ③ 批量填充关联数据（避免 N+1）
  if (page.hasContent()) {
    departmentQuery.setLeaderUser(page.getContent());
  }

  // ④ Page<Domain> → PageResult<VO>
  return buildVoPageResult(page, DepartmentAssembler::toDetailVo);
}
```

**第三步：Assembler 构建查询规格**

```java
// 接口层 Assembler - DTO → 查询 Specification
public static GenericSpecification<Department> getSpecification(DepartmentFindDto dto) {
  Set<SearchCriteria> filters = new SearchCriteriaBuilder<>(dto)
      .rangeSearchFields("id", "createdDate", "modifiedDate") // 支持范围查询（如 id[0]=1&id[1]=100）
      .orderByFields("id", "createdDate", "modifiedDate", "name", "code") // 允许排序的字段白名单
      .matchSearchFields("name", "code", "description")       // 全文搜索匹配字段
      .build();
  return new GenericSpecification<>(filters);
}
```

**第四步：Query 服务执行查询**

```java
// 应用层 Query - 执行分页查询（支持全文搜索切换）
@Override
public Page<Department> find(GenericSpecification<Department> spec, PageRequest pageable,
    boolean fullTextSearch, String[] match) {
  return new BizTemplate<Page<Department>>() {
    @Override
    protected Page<Department> process() {
      // fullTextSearch=true 时走全文搜索引擎，否则走普通 JPA 查询
      return fullTextSearch
          ? departmentSearchRepo.find(spec.getCriteria(), pageable, Department.class, match)
          : departmentRepo.findAll(spec, pageable);
    }
  }.execute();
}
```

**最终响应格式**

```json
{
  "code": "S",
  "message": "操作成功",
  "data": {
    "total": 25,
    "list": [
      {
        "id": 1001,
        "name": "研发部",
        "code": "RD",
        "status": "ENABLED",
        "leaderId": 500,
        "leaderName": "张三",
        "createdDate": "2025-01-10T10:00:00"
      },
      {
        "id": 1002,
        "name": "研发二部",
        "code": "RD2",
        "status": "ENABLED",
        "leaderId": 501,
        "leaderName": "李四",
        "createdDate": "2025-02-15T14:30:00"
      }
    ]
  },
  "datetime": "2026-03-16T09:00:00"
}
```

**请求参数示例（Query String）**

```
# 基础分页
GET /api/v1/departments?pageNo=1&pageSize=10

# 带排序（按名称升序）
GET /api/v1/departments?pageNo=1&pageSize=10&orderBy=name&orderSort=ASC

# 带筛选（启用状态）
GET /api/v1/departments?pageNo=1&pageSize=10&status=ENABLED

# 全文搜索（搜索 name/code/description 包含"研发"的记录）
GET /api/v1/departments?pageNo=1&pageSize=10&fullTextSearch=true&name=研发

# 范围查询（createdDate 在指定日期范围内）
GET /api/v1/departments?pageNo=1&pageSize=10&createdDate[0]=2025-01-01T00:00:00&createdDate[1]=2025-12-31T23:59:59
```

### 7.3 Controller 层触发校验

```java
// 请求体校验
public ApiLocaleResult<DepartmentDetailVo> create(@Valid @RequestBody DepartmentCreateDto dto)

// Query 参数校验（分页查询专用）
public ApiLocaleResult<PageResult<DepartmentDetailVo>> list(@Valid @ParameterObject DepartmentFindDto dto)
```

### 7.4 业务校验（BizTemplate.checkParams()）

```java
@Override
@Transactional(rollbackFor = Exception.class)
public Quota update(Quota quota) {
  return new BizTemplate<Quota>() {
    Quota existing;

    @Override
    protected void checkParams() {
      // 1. 查询资源是否存在
      existing = quotaQuery.findByCodeAndCheck(quota.getCode());
      // 2. 业务规则校验
      if (Boolean.TRUE.equals(existing.getIsLicenseControl())) {
        throw ProtocolException.of("配额「{0}」由许可控制，不允许修改",
            new Object[]{existing.getName()});
      }
      if (quota.getLimit() != null && quota.getLimit() < existing.getUsed()) {
        throw ProtocolException.of("配额限额不能小于当前已使用量「{0}」",
            new Object[]{existing.getUsed()});
      }
    }

    @Override
    protected Quota process() {
      existing.setLimit(quota.getLimit());
      return quotaRepo.save(existing);
    }
  }.execute();
}
```

**校验层级（由外到内）：**
1. **JSR-303**（Controller 层）：格式、非空、长度、范围
2. **业务校验**（BizTemplate.checkParams()）：资源存在性、业务规则一致性
3. **数据库约束**（最后保障）：唯一键、非空约束

---

## 8. 异常分类与全局处理

### 8.1 异常类型（AngusInfra 提供）

| 异常类 | 包路径 | HTTP 状态码 | 使用场景 |
|--------|-------|-----------|---------|
| `ProtocolException` | `cloud.xcan.angus.remote.message` | 400 | 业务规则违反、参数逻辑错误 |
| `ResourceNotFound` | `cloud.xcan.angus.remote.message.http` | 404 | 资源不存在 |
| `ResourceExisted` | `cloud.xcan.angus.remote.message.http` | 409 | 资源已存在（重复创建） |
| `BizException` | `cloud.xcan.angus.remote.message` | 400 | 通用业务异常 |
| `ForbiddenException` | `cloud.xcan.angus.remote.message.http` | 403 | 无权访问 |

> **全局异常处理由 AngusInfra 框架统一提供，项目无需自定义 `@ControllerAdvice`。**

### 8.2 抛出异常示例

```java
// 资源不存在 → 404
return quotaRepo.findByTenantIdAndCode(tenantId, code)
    .orElseThrow(() -> ResourceNotFound.of("资源配额编码「{0}」不存在", new Object[]{code}));

// 简便写法（id + 资源类型名）
departmentRepo.findById(id)
    .orElseThrow(() -> ResourceNotFound.of(id, "Department"));

// 业务规则违反 → 400
if (Boolean.TRUE.equals(existing.getIsLicenseControl())) {
  throw ProtocolException.of("配额「{0}」由许可控制，不允许修改",
      new Object[]{existing.getName()});
}

// 使用 ProtocolAssert 断言（AngusInfra）
assertResourceNotFound(department != null, id, "Department");
```

### 8.3 捕获异常时的处理原则

```java
try {
  doSomething();
} catch (MessagingException e) {
  log.error("SMTP连接测试失败", e);  // 错误级别，带完整堆栈
  vo.setConnected(false);
  vo.setMessage("连接失败: " + e.getMessage());
} catch (Exception e) {
  log.error("操作发生异常", e);      // 兜底异常
  throw ProtocolException.of("系统异常，请稍后重试");
}
```

---

## 9. 错误码体系

### 9.1 成功码

| 代码 | 含义 |
|------|------|
| `S` | 成功（Success） |

### 9.2 错误码分类（AngusInfra `ApiConstant.ECode`）

| 错误码前缀 | 含义 | HTTP 状态 |
|-----------|------|----------|
| `B_PROTOCOL_*` | 协议/业务异常（ProtocolException） | 400 |
| `B_NOT_FOUND_*` | 资源未找到（ResourceNotFound） | 404 |
| `B_EXISTED_*` | 资源已存在（ResourceExisted） | 409 |
| `B_FORBIDDEN_*` | 权限不足（ForbiddenException） | 403 |
| `B_BIZ_*` | 通用业务异常（BizException） | 400 |
| `S_*` | 系统内部错误 | 500 |

### 9.3 错误消息规范

- 消息支持参数占位符：`"配额「{0}」的限额不能小于已使用量「{1}」"`
- 参数数组：`new Object[]{quota.getName(), quota.getUsed()}`
- 支持国际化（由 AngusInfra 的 `MessageHolder` 处理）

### 9.4 `ext.eKey`（机器可读错误标识）

`eKey` 是机器可读的错误标识符，存放于响应 `ext.eKey`，便于客户端程序化处理。

```json
{
  "code": "B_PROTOCOL_001",
  "message": "用户已被禁用",
  "ext": { "eKey": "USER_DISABLED" }
}
```

---

## 10. 安全要求

### 10.1 认证机制

- 所有接口默认通过 **OAuth2 Bearer Token** 认证（由 AngusInfra Spring Security 统一配置）
- 内部接口（`/innerapi/**`）使用 `SCOPE_inner_api_trust` 授权

### 10.2 鉴权注解

```java
// 仅运营客户端可访问
@PreAuthorize("@PPS.isOpClient()")
@RestController
public class AuthenticationClientRest { ... }

// 内部服务间调用
@PreAuthorize("hasAuthority('SCOPE_inner_api_trust')")
@RestController
public class AuthenticationClientInnerRest { ... }

// 条件激活（仅 Cloud 版）
@Conditional(value = CloudServiceEditionCondition.class)
```

### 10.3 多租户隔离

- 所有实体继承 `TenantAuditingEntity`，自动隔离租户数据
- `BizTemplate` 默认开启多租户控制（`multiTenantCtrl = true`）
- 跨租户操作必须显式关闭并手动处理：

```java
// 跨租户查询（谨慎使用，必须手动过滤 tenantId）
return new BizTemplate<List<Quota>>(false) {
  @Override
  protected List<Quota> process() {
    return quotaRepo.findByTenantId(tenantId);
  }
}.execute();
```

### 10.4 SQL 注入防护

```java
// ✅ 正确：使用命名参数
@Query("SELECT e FROM Email e WHERE e.templateId = :templateId")
long countByTemplateId(@Param("templateId") Long templateId);

// ❌ 错误：字符串拼接（SQL 注入风险！）
// @Query("SELECT e FROM Email e WHERE e.templateId = " + templateId)
```

禁止在 JPQL/SQL 中拼接字符串，统一使用 JPA Specification 或 `@Param` 命名参数。

### 10.5 敏感数据脱敏

- 密码字段存储前必须加密，响应 VO 中不返回或返回掩码
- SMTP 密码、API Secret 等敏感信息在 VO 中处理：`@JsonIgnore` 或脱敏值
- 用户手机号、邮箱按业务需要在 VO 中脱敏

### 10.6 细粒度权限校验

```java
import cloud.xcan.angus.core.biz.PermissionCheck;

// 检查云租户安全性（仅允许云版本实例访问特定配置）
PermissionCheck.checkCloudTenantSecurity();
```

---

## 11. 日志级别与内容要求

### 11.1 日志框架

使用 Lombok `@Slf4j` 注解（SLF4J + Logback）：

```java
@Slf4j
@Service
public class QuotaCmdImpl extends CommCmd<Quota, Long> implements QuotaCmd { ... }
```

### 11.2 日志级别规范

| 级别 | 使用场景 |
|------|---------|
| `ERROR` | 系统/业务异常，需立即关注（如连接失败、数据库异常） |
| `WARN` | 非预期但不影响主流程（如无效参数跳过、配额不足告警） |
| `INFO` | 重要业务流程节点（初始化完成、重要配置加载、关键业务操作） |
| `DEBUG` | 详细调试信息（生产环境关闭） |

### 11.3 日志内容规范

```java
// ✅ 正确：包含关键业务参数
log.info("根据模板配额创建新配额：tenantId={}, quotaCode={}, quotaName={}",
    tenantId, templateQuota.getCode(), templateQuota.getName());

log.warn("配额减少量无效，跳过操作：tenantId={}, quotaCode={}, amount={}",
    mainTenantId, quotaCode, amount);

log.error("SMTP连接测试失败", e);  // 错误日志必须带完整异常堆栈

// ❌ 错误：缺少上下文
log.error("操作失败");

// ❌ 错误：打印敏感信息（违规！）
log.info("用户登录：username={}, password={}", username, password);
```

### 11.4 操作日志（业务审计）

```java
// 关键业务操作必须记录到审计日志
userOperationLogCmd.logSuccessByMessageKey(
    OperationAction.UPDATE,          // 操作类型：READ/CREATE/UPDATE/DELETE
    ResourceType.QUOTA,              // 资源类型（枚举）
    saved.getId(),                   // 资源ID
    saved.getName(),                 // 资源名称
    OperationMessage.QUOTA_UPDATE_DETAILS,  // 消息模板 Key
    new Object[]{saved.getName(), saved.getLimit(), saved.getUnit()}
);
```

---

## 12. 数据库 / 表 / 字段命名规范

### 12.1 表命名规则

| 规则 | 示例 |
|------|------|
| 全小写下划线分隔 | `gm_email_smtp` |
| 以服务名缩写为前缀 | `gm_` 前缀 |
| 使用单数形式 | `gm_email_template`（非 `templates`） |
| 关联表用两个实体名组合 | `gm_department_user` |

**实际项目表名示例：**
```
gm_department           -- 部门
gm_department_user      -- 部门用户关联
gm_group                -- 组
gm_group_user           -- 组用户关联
gm_role                 -- 角色
gm_email                -- 邮件
gm_email_smtp           -- SMTP 配置
gm_email_template       -- 邮件模板
gm_email_tracking       -- 邮件追踪
```

### 12.2 字段命名规则

| 规则 | 示例 |
|------|------|
| 全小写下划线分隔 | `created_date`, `parent_id` |
| 主键统一用 `id`（BIGINT） | `id BIGINT` |
| 外键用 `{entity}_id` | `tenant_id`, `owner_id`, `parent_id` |
| 布尔值用 `is_` 前缀 | `is_default`, `is_system` |
| 时间字段用 `_date`/`_time` | `created_date`, `send_time` |
| 审计字段（框架自动管理） | `created_by`, `created_date`, `modified_by`, `modified_date`, `tenant_id` |

### 12.3 JPA 实体规范

```java
@Setter
@Getter
@Entity
@Table(name = "gm_email_template")       // 表名：gm_前缀 + 小写下划线
public class EmailTemplate extends AuditingEntity<EmailTemplate, Long> {

  @Id
  private Long id;                        // Long 类型主键（Snowflake ID）

  @Column(name = "name", nullable = false, length = 100)
  private String name;

  @Enumerated(EnumType.STRING)            // 枚举存储为字符串
  @Column(name = "status", length = 20)
  private EnabledStatus status;

  @Type(JsonType.class)                   // JSON 字段
  @Column(name = "params", columnDefinition = "json")
  private List<String> params;

  @Column(name = "content", columnDefinition = "TEXT")  // 大文本
  private String content;

  @Transient                              // 非持久化字段
  private Integer emailCount;

  @Override
  public Long identity() { return id; }
}
```

### 12.4 实体基类选择规则

| 基类 | 包路径 | 包含字段 | 使用场景 |
|------|-------|---------|---------|
| `TenantAuditingEntity<T, ID>` | `cloud.xcan.angus.core.jpa.multitenancy` | `tenant_id` + 4个审计字段 | 多租户 + 审计（主要场景） |
| `TenantEntity<T, ID>` | `cloud.xcan.angus.core.jpa.multitenancy` | 仅 `tenant_id` | 多租户（无审计） |
| `AuditingEntity<T, ID>` | `cloud.xcan.angus.core.jpa.auditor` | 仅4个审计字段 | 非租户 + 审计 |

---

## 13. 事务管理规则

### 13.1 事务注解位置

| 位置 | 注解 | 说明 |
|------|------|------|
| Cmd 方法（写操作） | `@Transactional(rollbackFor = Exception.class)` | **必须加** |
| QueryImpl 类级别 | `@Transactional(readOnly = true)` | **必须加** |
| Manager 写方法 | `@Transactional(rollbackFor = Exception.class)` | 跨模块写操作 |

```java
// ✅ Cmd 实现：每个写方法添加事务
@Slf4j
@Service
public class QuotaCmdImpl extends CommCmd<Quota, Long> implements QuotaCmd {

  @Override
  @Transactional(rollbackFor = Exception.class)
  public Quota update(Quota quota) { ... }

  @Override
  @Transactional(rollbackFor = Exception.class)
  public List<Quota> batchUpdateLimits(List<String> codes, List<Long> limits) { ... }
}

// ✅ Query 实现：类级别只读事务
@Service
@Transactional(readOnly = true)
public class QuotaQueryImpl implements QuotaQuery { ... }
```

### 13.2 禁止事项

```java
// ❌ Facade 层不能加事务
@Transactional  // 错误！
public DepartmentDetailVo create(DepartmentCreateDto dto) { ... }

// ❌ Query 类不能加写事务
@Service
@Transactional(rollbackFor = Exception.class)  // 错误！应为 readOnly
public class DepartmentQueryImpl { ... }

// ❌ Controller 层不能加事务
@Transactional  // 错误！
@PostMapping
public ApiLocaleResult<...> create(...) { ... }
```

### 13.3 事务传播规则

- 默认传播行为：`REQUIRED`（加入已有事务，无则创建）
- 特殊情况（日志记录等需独立提交）：`REQUIRES_NEW`

---

## 14. API 文档（Swagger/OpenAPI 注解要求）

### 14.1 必须加的注解

**Controller 类：**
```java
@Tag(name = "Department", description = "部门管理 - 部门的创建、管理等功能")
@Validated
@RestController
@RequestMapping("/api/v1/departments")
```

**Controller 方法：**
```java
@Operation(
    operationId = "createDepartment",   // 必须：全局唯一（camelCase）
    summary = "创建部门",               // 必须：简短摘要
    description = "创建新部门"          // 可选：详细说明
)
@ApiResponses(value = {
    @ApiResponse(responseCode = "201", description = "部门创建成功"),
    @ApiResponse(responseCode = "400", description = "参数错误")
})
@ResponseStatus(HttpStatus.CREATED)
@PostMapping
```

**Path 参数：**
```java
@Parameter(description = "部门ID") @PathVariable Long id
```

**DTO/VO 类和字段：**
```java
@Schema(description = "创建部门请求参数")
public class DepartmentCreateDto {
  @Schema(description = "部门名称", requiredMode = RequiredMode.REQUIRED)
  private String name;
}
```

### 14.2 `operationId` 命名规范

| 操作 | 格式 | 示例 |
|------|------|------|
| 创建 | `create{Module}` | `createDepartment` |
| 完整更新 | `update{Module}` | `updateDepartment` |
| 部分更新 | `update{Module}{Action}` | `updateDepartmentStatus` |
| 删除 | `delete{Module}` | `deleteDepartment` |
| 详情 | `get{Module}Detail` | `getDepartmentDetail` |
| 列表 | `get{Module}List` | `getDepartmentList` |
| 统计 | `get{Module}Stats` | `getDepartmentStats` |

### 14.3 GET 查询参数的 Swagger 绑定

```java
// ✅ GET 请求的查询 DTO 必须用 @ParameterObject（展开为独立 Query Params）
@GetMapping
public ApiLocaleResult<PageResult<DepartmentDetailVo>> list(
    @Valid @ParameterObject DepartmentFindDto dto) { ... }

// ❌ 错误：GET 请求不能用 @RequestBody
```

---

## 15. 禁止事项清单

### 15.1 架构相关

| # | 禁止行为 | 原因 |
|---|---------|------|
| 1 | Controller（接口层）直接调用 Cmd/Query/Manager/Repository | 接口层只能调用门面层（Facade），不得越级 |
| 2 | Controller 包含业务逻辑 | 职责不单一 |
| 3 | Facade 层直接调用 Repository（基础设施层） | 门面层必须通过应用层（Cmd/Query）访问数据 |
| 4 | Facade 层加 `@Transactional` | 事务边界应在 Cmd 层 |
| 5 | 领域实体注入 Spring Bean | 污染领域模型 |
| 6 | 跨服务直接调用 Repository | 应通过 API/Remote 接口 |
| 7 | Query 类执行写操作 | 违反 CQRS 原则 |
| 8 | 在应用层（Cmd/Query）使用普通 DTO（CreateDto/UpdateDto/FindDto） | DTO 只属于接口层，统计 VO 特例除外 |
| 9 | 在基础设施层（RepoMysql/RepoPostgres）引用任何 DTO 或 VO | 基础设施层只依赖领域层 |

### 15.2 数据访问相关

| # | 禁止行为 | 替代方案 |
|---|---------|---------|
| 10 | 拼接 SQL 字符串 | JPA Specification 或命名参数 |
| 11 | 循环内查数据库（N+1） | 批量查询 + Map 组装 |
| 12 | 事务内执行远程 HTTP 调用 | 事务外调用，或异步/消息 |
| 13 | 使用 `@GeneratedValue` 自增主键 | `uidGenerator.getUID()`（Snowflake） |

### 15.3 安全相关

| # | 禁止行为 |
|---|---------|
| 14 | 日志打印密码、Token、密钥 |
| 15 | 响应体返回原始密码/Token |
| 16 | 硬编码 Token/Secret 到代码 |
| 17 | 未鉴权的批量操作接口 |

### 15.4 代码质量相关

| # | 禁止行为 | 替代方案 |
|---|---------|---------|
| 18 | 硬编码魔法数字（如 `@Size(max=100)`） | `BizConstant.MAX_NAME_LENGTH` |
| 19 | 忽略 `@Valid` 注解 | Controller 参数必须加 `@Valid` |
| 20 | 缺少 `@ResponseStatus` | 每个接口显式声明 HTTP 状态码 |
| 21 | DELETE 接口有返回值 | DELETE 返回 `void`，状态码 204 |
| 22 | 缺少 `@Operation`、`@ApiResponse` | 所有 public 接口必须有文档注解 |
| 23 | 使用 `@Autowired` 注入 | 使用 `@Resource`（JSR-250） |
| 24 | 捕获异常后静默吞没（空 catch） | 至少 `log.error`，必要时重新抛出 |
| 25 | Query DTO 不继承 `PageQuery` | 分页 DTO 必须继承 `PageQuery` |
| 26 | 未重写 `getDefaultOrderBy()` | `PageQuery` 子类必须重写 |

---

## 16. 标准代码样板

> **以下代码均来自 AngusRepo 实际实现，可直接作为 AI 代码生成的参考模板。**

### 16.1 REST 控制器样板

```java
package cloud.xcan.angus.core.gm.interfaces.department;

import cloud.xcan.angus.api.gm.EnabledStatusUpdateDto;
import cloud.xcan.angus.core.gm.interfaces.department.facade.DepartmentFacade;
import cloud.xcan.angus.core.gm.interfaces.department.facade.dto.DepartmentCreateDto;
import cloud.xcan.angus.core.gm.interfaces.department.facade.dto.DepartmentFindDto;
import cloud.xcan.angus.core.gm.interfaces.department.facade.dto.DepartmentUpdateDto;
import cloud.xcan.angus.core.gm.interfaces.department.facade.vo.DepartmentDetailVo;
import cloud.xcan.angus.remote.ApiLocaleResult;
import cloud.xcan.angus.remote.PageResult;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.responses.ApiResponses;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.annotation.Resource;
import jakarta.validation.Valid;
import org.springdoc.core.annotations.ParameterObject;
import org.springframework.http.HttpStatus;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;

@Tag(name = "Department", description = "部门管理 - 部门的创建、管理等功能")
@Validated
@RestController
@RequestMapping("/api/v1/departments")
public class DepartmentRest {

  @Resource
  private DepartmentFacade departmentFacade;

  @Operation(operationId = "createDepartment", summary = "创建部门")
  @ApiResponses(value = {@ApiResponse(responseCode = "201", description = "部门创建成功")})
  @ResponseStatus(HttpStatus.CREATED)
  @PostMapping
  public ApiLocaleResult<DepartmentDetailVo> create(@Valid @RequestBody DepartmentCreateDto dto) {
    return ApiLocaleResult.success(departmentFacade.create(dto));
  }

  @Operation(operationId = "updateDepartment", summary = "更新部门")
  @ApiResponses(value = {@ApiResponse(responseCode = "200", description = "更新成功")})
  @ResponseStatus(HttpStatus.OK)
  @PutMapping("/{id}")
  public ApiLocaleResult<DepartmentDetailVo> update(
      @Parameter(description = "部门ID") @PathVariable Long id,
      @Valid @RequestBody DepartmentUpdateDto dto) {
    return ApiLocaleResult.success(departmentFacade.update(id, dto));
  }

  @Operation(operationId = "updateDepartmentStatus", summary = "禁用/激活部门")
  @ApiResponses(value = {@ApiResponse(responseCode = "200", description = "状态更新成功")})
  @ResponseStatus(HttpStatus.OK)
  @PatchMapping("/{id}/status")
  public ApiLocaleResult<DepartmentDetailVo> updateStatus(
      @Parameter(description = "部门ID") @PathVariable Long id,
      @Valid @RequestBody EnabledStatusUpdateDto dto) {
    return ApiLocaleResult.success(departmentFacade.updateStatus(id, dto));
  }

  @Operation(operationId = "deleteDepartment", summary = "删除部门")
  @ApiResponses(value = {@ApiResponse(responseCode = "204", description = "删除成功")})
  @ResponseStatus(HttpStatus.NO_CONTENT)
  @DeleteMapping("/{id}")
  public void delete(@Parameter(description = "部门ID") @PathVariable Long id) {
    departmentFacade.delete(id);
  }

  @Operation(operationId = "getDepartmentDetail", summary = "获取部门详情")
  @ApiResponses(value = {@ApiResponse(responseCode = "200", description = "部门详情获取成功")})
  @ResponseStatus(HttpStatus.OK)
  @GetMapping("/{id}")
  public ApiLocaleResult<DepartmentDetailVo> getDetail(
      @Parameter(description = "部门ID") @PathVariable Long id) {
    return ApiLocaleResult.success(departmentFacade.getDetail(id));
  }

  @Operation(operationId = "getDepartmentList", summary = "获取部门列表")
  @ApiResponses(value = {@ApiResponse(responseCode = "200", description = "部门列表获取成功")})
  @ResponseStatus(HttpStatus.OK)
  @GetMapping
  public ApiLocaleResult<PageResult<DepartmentDetailVo>> list(
      @Valid @ParameterObject DepartmentFindDto dto) {
    return ApiLocaleResult.success(departmentFacade.list(dto));
  }
}
```

### 16.2 Facade 门面样板

```java
// --- 门面接口 ---
public interface DepartmentFacade {
  DepartmentDetailVo create(DepartmentCreateDto dto);
  DepartmentDetailVo update(Long id, DepartmentUpdateDto dto);
  DepartmentDetailVo updateStatus(Long id, EnabledStatusUpdateDto dto);
  void delete(Long id);
  DepartmentDetailVo getDetail(Long id);
  PageResult<DepartmentDetailVo> list(DepartmentFindDto dto);
}

// --- 门面实现 ---
@Component
public class DepartmentFacadeImpl implements DepartmentFacade {

  @Resource
  private DepartmentCmd departmentCmd;
  @Resource
  private DepartmentQuery departmentQuery;

  @NameJoin
  @Override
  public DepartmentDetailVo create(DepartmentCreateDto dto) {
    Department domain = DepartmentAssembler.toCreateDomain(dto);
    Department saved = departmentCmd.create(domain);
    return DepartmentAssembler.toDetailVo(saved);
  }

  @NameJoin
  @Override
  public PageResult<DepartmentDetailVo> list(DepartmentFindDto dto) {
    GenericSpecification<Department> spec = DepartmentAssembler.getSpecification(dto);
    Page<Department> page = departmentQuery.find(spec, dto.tranPage(),
        dto.fullTextSearch, getMatchSearchFields(dto.getClass()));
    if (page.hasContent()) {
      departmentQuery.setLeaderUser(page.getContent());
    }
    return buildVoPageResult(page, DepartmentAssembler::toDetailVo);
  }
}
```

### 16.3 Cmd 实现样板

```java
@Slf4j
@Service
public class DepartmentCmdImpl extends CommCmd<Department, Long> implements DepartmentCmd {

  @Resource
  private DepartmentRepo departmentRepo;
  @Resource
  private DepartmentQuery departmentQuery;
  @Resource
  private UserOperationLogCmd userOperationLogCmd;

  @Override
  @Transactional(rollbackFor = Exception.class)
  public Department create(Department department) {
    return new BizTemplate<Department>() {
      @Override
      protected void checkParams() {
        if (departmentRepo.existsByCode(department.getCode())) {
          throw ProtocolException.of("部门编码「{0}」已存在",
              new Object[]{department.getCode()});
        }
      }

      @Override
      protected Department process() {
        department.setId(uidGenerator.getUID());
        Department saved = departmentRepo.save(department);
        userOperationLogCmd.logSuccessByMessageKey(
            OperationAction.CREATE, ResourceType.DEPARTMENT,
            saved.getId(), saved.getName(), "dept.created", null);
        return saved;
      }
    }.execute();
  }

  @Override
  protected BaseRepository<Department, Long> getRepository() {
    return departmentRepo;
  }
}
```

### 16.4 Query 实现样板

```java
@Service
@Transactional(readOnly = true)
public class DepartmentQueryImpl implements DepartmentQuery {

  @Resource
  private DepartmentRepo departmentRepo;
  @Resource
  private DepartmentSearchRepo departmentSearchRepo;
  @Resource
  private UserManager userManager;

  @Override
  public Department findByIdAndCheck(Long id) {
    return new BizTemplate<Department>() {
      @Override
      protected Department process() {
        return departmentRepo.findById(id)
            .orElseThrow(() -> ResourceNotFound.of(id, "Department"));
      }
    }.execute();
  }

  @Override
  public Page<Department> find(GenericSpecification<Department> spec, PageRequest pageable,
      boolean fullTextSearch, String[] match) {
    return new BizTemplate<Page<Department>>() {
      @Override
      protected Page<Department> process() {
        return fullTextSearch
            ? departmentSearchRepo.find(spec.getCriteria(), pageable, Department.class, match)
            : departmentRepo.findAll(spec, pageable);
      }
    }.execute();
  }

  @Override
  public void setLeaderUser(List<Department> departments) {
    Set<Long> leaderIds = departments.stream()
        .filter(d -> d.getLeaderId() != null)
        .map(Department::getLeaderId)
        .collect(Collectors.toSet());
    if (leaderIds.isEmpty()) return;
    Map<Long, UserBase> userMap = userManager.getUserBaseMap(leaderIds);
    departments.forEach(d -> {
      if (d.getLeaderId() != null) {
        UserBase leader = userMap.get(d.getLeaderId());
        if (leader != null) d.setLeaderName(leader.getFullName());
      }
    });
  }
}
```

### 16.5 DTO 和 VO 样板

```java
// --- 创建 DTO ---
@Getter
@Setter
@Accessors(chain = true)
@Schema(description = "创建部门请求参数")
public class DepartmentCreateDto implements Serializable {

  @NotBlank
  @Size(max = MAX_NAME_LENGTH)
  @Schema(description = "部门名称", requiredMode = RequiredMode.REQUIRED)
  private String name;

  @NotBlank
  @Size(max = MAX_CODE_LENGTH)
  @Schema(description = "部门编码", requiredMode = RequiredMode.REQUIRED)
  private String code;

  @Size(max = MAX_DESC_LENGTH)
  @Schema(description = "部门描述")
  private String description;

  @NotNull
  @Schema(description = "上级部门ID，根部门填0", requiredMode = RequiredMode.REQUIRED)
  private Long parentId;
}

// --- 查询 DTO ---
@Data
@EqualsAndHashCode(callSuper = true)
@Schema(description = "查询部门请求参数")
public class DepartmentFindDto extends PageQuery {

  @Schema(description = "部门ID")
  private Long id;

  @Schema(description = "部门名称")
  private String name;

  @Schema(description = "状态：ENABLED-启用, DISABLED-禁用")
  private EnabledStatus status;

  @Override
  public String getDefaultOrderBy() {
    return "createdDate";
  }
}

// --- 详情 VO ---
@Getter
@Setter
@Accessors(chain = true)
@Schema(description = "部门详情")
public class DepartmentDetailVo implements Serializable {

  @Schema(description = "部门ID")
  private Long id;

  @Schema(description = "部门名称")
  private String name;

  @Schema(description = "部门编码")
  private String code;

  @Schema(description = "状态：ENABLED-启用, DISABLED-禁用")
  private EnabledStatus status;

  @Schema(description = "负责人ID")
  private Long leaderId;

  @Schema(description = "负责人姓名")
  private String leaderName;

  @Schema(description = "创建人ID")
  private Long createdBy;

  @NameJoinField(id = "createdBy", repository = "commonUserBaseRepo")
  @Schema(description = "创建人姓名")
  private String createdByName;

  @Schema(description = "创建时间")
  private LocalDateTime createdDate;
}
```

### 16.6 领域实体样板

```java
@Setter
@Getter
@Entity
@Table(name = "gm_department")
public class Department extends TenantAuditingEntity<Department, Long> {

  @Id
  private Long id;

  @Column(name = "name", nullable = false, length = 100)
  private String name;

  @Column(name = "code", nullable = false, length = 50, unique = true)
  private String code;

  @Column(name = "description", length = 500)
  private String description;

  @Enumerated(EnumType.STRING)
  @Column(name = "status", length = 20)
  private EnabledStatus status;

  @Column(name = "parent_id")
  private Long parentId;

  @Column(name = "leader_id")
  private Long leaderId;

  @Transient
  private String leaderName;
  @Transient
  private Long userCount;

  @Override
  public Long identity() {
    return id;
  }
}
```

### 16.7 仓储接口样板

```java
// --- 领域层仓储接口 ---
@NoRepositoryBean
public interface DepartmentRepo extends BaseRepository<Department, Long> {

  boolean existsByCode(String code);
  boolean existsByParentId(Long parentId);
  List<Department> findByParentId(Long parentId);

  @Query("SELECT d FROM Department d WHERE d.tenantId = :tenantId AND d.status = :status")
  List<Department> findByTenantIdAndStatus(
      @Param("tenantId") Long tenantId,
      @Param("status") EnabledStatus status);
}

// --- MySQL 实现 ---
@Repository("departmentRepo")
public interface DepartmentRepoMysql extends DepartmentRepo { }

// --- PostgreSQL 实现 ---
@Repository("departmentRepo")
public interface DepartmentRepoPostgres extends DepartmentRepo { }
```

### 16.8 Assembler 样板

```java
public class DepartmentAssembler {

  public static Department toCreateDomain(DepartmentCreateDto dto) {
    Department domain = new Department();
    domain.setName(dto.getName());
    domain.setCode(dto.getCode());
    domain.setDescription(dto.getDescription());
    domain.setParentId(dto.getParentId());
    domain.setStatus(EnabledStatus.ENABLED);
    return domain;
  }

  public static DepartmentDetailVo toDetailVo(Department dept) {
    DepartmentDetailVo vo = new DepartmentDetailVo();
    vo.setId(dept.getId());
    vo.setName(dept.getName());
    vo.setCode(dept.getCode());
    vo.setStatus(dept.getStatus());
    vo.setLeaderId(dept.getLeaderId());
    vo.setLeaderName(dept.getLeaderName());
    vo.setCreatedBy(dept.getCreatedBy());
    vo.setCreatedDate(dept.getCreatedDate());
    return vo;
  }

  public static GenericSpecification<Department> getSpecification(DepartmentFindDto dto) {
    Set<SearchCriteria> filters = new SearchCriteriaBuilder<>(dto)
        .rangeSearchFields("id", "createdDate", "modifiedDate")
        .orderByFields("id", "createdDate", "modifiedDate", "name", "code")
        .matchSearchFields("name", "code", "description")
        .build();
    return new GenericSpecification<>(filters);
  }
}
```

### 16.9 Remote（FeignClient）样板

```java
@FeignClient(name = "${xcan.service.gm:XCAN-ANGUSGM.BOOT}")
public interface DepartmentRemote {

  @Operation(operationId = "createDepartment", summary = "创建部门")
  @ApiResponses(value = {@ApiResponse(responseCode = "201", description = "创建成功")})
  @ResponseStatus(HttpStatus.CREATED)
  @PostMapping("/api/v1/departments")
  ApiLocaleResult<DepartmentDetailVo> create(@Valid @RequestBody DepartmentCreateDto dto);

  @Operation(operationId = "getDepartmentDetail", summary = "获取部门详情")
  @ApiResponses(value = {@ApiResponse(responseCode = "200", description = "部门详情获取成功")})
  @ResponseStatus(HttpStatus.OK)
  @GetMapping("/api/v1/departments/{id}")
  ApiLocaleResult<DepartmentDetailVo> getDetail(
      @Parameter(description = "部门ID") @PathVariable Long id);
}
```

---

## 17. 规范文档结构说明（AI 可解析）

### 17.1 AI 代码生成验证清单

当 AI 生成 AngusRepo 项目代码时，必须逐项验证：

```yaml
# AngusRepo AI 代码生成验证清单 v2.0
rest_controller:
  class_name: ends_with "Rest"
  annotations: ["@Tag", "@Validated", "@RestController", "@RequestMapping"]
  base_path: starts_with "/api/v1/"
  dependency_injection: uses "@Resource" (NOT "@Autowired")
  calls_only: Facade layer methods (NEVER Cmd/Query/Manager/Repository directly)
  methods:
    all: has ["@Operation(operationId)", "@ApiResponses", "@ResponseStatus", "@{Verb}Mapping"]
    POST_create: status=201, returns ApiLocaleResult<XxxDetailVo>
    PUT_update: status=200, returns ApiLocaleResult<XxxDetailVo>
    PATCH_partial: status=200, returns ApiLocaleResult<XxxVo>
    DELETE: status=204, return_type=void, no_response_body
    GET_detail: status=200, returns ApiLocaleResult<XxxDetailVo>
    GET_list: status=200, returns ApiLocaleResult<PageResult<XxxVo>>, uses @ParameterObject
    all_write_and_read: "@Valid" on @RequestBody and @ParameterObject params

dto:
  create_dto: [implements Serializable, has @Schema on class, field validations present]
  find_dto: [extends PageQuery, overrides getDefaultOrderBy()]
  length_limits: uses BizConstant constants (NOT magic numbers)
  swagger: all fields have @Schema

vo:
  class_level: has @Schema
  all_fields: has @Schema
  sensitive_fields: no raw passwords/tokens returned
  name_join: uses @NameJoinField(id="...", repository="...")

entity:
  extends: one of [TenantAuditingEntity, TenantEntity, AuditingEntity]
  table_name: [starts_with "gm_", all_lowercase_snake_case]
  primary_key: Long id (Snowflake, assigned by uidGenerator)
  enum_fields: @Enumerated(EnumType.STRING)
  json_fields: [@Type(JsonType.class), columnDefinition="json"]
  non_persistent: @Transient for computed/related data
  implements: identity() returning id

repository_interface:
  annotation: @NoRepositoryBean
  extends: BaseRepository<Entity, Long>
  query_params: uses @Param named parameters (NO string concatenation)

repository_impl:
  mysql: [@Repository("xxxRepo"), extends domain interface, empty body]
  postgres: [@Repository("xxxRepo"), extends domain interface, empty body]

cmd_impl:
  annotations: ["@Slf4j", "@Service"]
  extends: CommCmd<Entity, Long>
  write_methods: all have @Transactional(rollbackFor = Exception.class)
  uses_biz_template: checkParams() for validation, process() for business logic
  id_generation: uidGenerator.getUID()
  audit_log: userOperationLogCmd.logSuccessByMessageKey(...)

query_impl:
  annotations: ["@Service", "@Transactional(readOnly = true)"]
  not_found_exception: ResourceNotFound.of(id, "ResourceName")
  paginated: supports fullTextSearch (switch between searchRepo and repo)
  batch_load: setXxxUser() for related data (avoids N+1)
  stats_vo_exception: statistics methods (getStatistics/getUsage) MAY return XxxStatisticsVo or XxxUsageVo (interface-layer VOs)
  no_dto: NEVER uses CreateDto/UpdateDto/FindDto (only statistics VOs allowed as exception)

facade_impl:
  annotation: @Component
  no_transactional: NEVER annotated with @Transactional
  no_direct_repo: NEVER injects or calls Repository (Repo) classes directly
  name_join: @NameJoin on methods returning VOs with name fields
  pagination: uses buildVoPageResult(page, Assembler::toDetailVo)

security:
  no_sql_injection: no string concatenation in @Query
  no_sensitive_logs: passwords/tokens never in log statements
  inner_api: uses @PreAuthorize("hasAuthority('SCOPE_inner_api_trust')")
  op_only_api: uses @PreAuthorize("@PPS.isOpClient()")
```

### 17.2 关键类型速查表（AngusInfra）

| 功能 | 类/注解 | 完整路径 |
|------|--------|---------|
| 统一响应 | `ApiLocaleResult<T>` | `cloud.xcan.angus.remote.ApiLocaleResult` |
| 分页结果 | `PageResult<T>` | `cloud.xcan.angus.remote.PageResult` |
| 分页查询基类 | `PageQuery` | `cloud.xcan.angus.remote.PageQuery` |
| 业务流程模板 | `BizTemplate<T>` | `cloud.xcan.angus.core.biz.BizTemplate` |
| 命令基类 | `CommCmd<T, ID>` | `cloud.xcan.angus.core.biz.cmd.CommCmd` |
| 基础仓储接口 | `BaseRepository<T, ID>` | `cloud.xcan.angus.core.jpa.repository.BaseRepository` |
| 全文搜索仓储 | `CustomBaseRepository<T>` | `cloud.xcan.angus.core.jpa.repository.CustomBaseRepository` |
| 多租户+审计实体 | `TenantAuditingEntity<T, ID>` | `cloud.xcan.angus.core.jpa.multitenancy.TenantAuditingEntity` |
| 仅审计实体 | `AuditingEntity<T, ID>` | `cloud.xcan.angus.core.jpa.auditor.AuditingEntity` |
| 资源不存在异常 | `ResourceNotFound` | `cloud.xcan.angus.remote.message.http.ResourceNotFound` |
| 业务规则异常 | `ProtocolException` | `cloud.xcan.angus.remote.message.ProtocolException` |
| 名称填充注解 | `@NameJoin` | `cloud.xcan.angus.core.biz.NameJoin` |
| 名称填充字段 | `@NameJoinField` | `cloud.xcan.angus.remote.NameJoinField` |
| 业务常量 | `BizConstant` | `cloud.xcan.angus.spec.experimental.BizConstant` |
| 查询规格 | `GenericSpecification<T>` | `cloud.xcan.angus.core.jpa.criteria.GenericSpecification` |
| 查询构建器 | `SearchCriteriaBuilder<T>` | `cloud.xcan.angus.core.jpa.criteria.SearchCriteriaBuilder` |
| 分页工具 | `CoreUtils.buildVoPageResult` | `cloud.xcan.angus.core.utils.CoreUtils` |
| 全文字段 | `getMatchSearchFields` | `cloud.xcan.angus.core.jpa.criteria.SearchCriteriaBuilder` |

### 17.3 快速开发步骤（新模块）

```
新模块开发顺序（严格按此顺序）：
1. 领域层：{Module}.java (实体) → {Module}Repo.java (仓储接口)
2. 基础设施：{Module}RepoMysql.java + {Module}RepoPostgres.java
3. 接口层 DTO/VO：{Module}CreateDto + {Module}FindDto + {Module}DetailVo
4. 接口层 Assembler：{Module}Assembler (toCreate/Update/Detail/Spec)
5. 应用层 Cmd：{Module}Cmd (接口) + {Module}CmdImpl (@Transactional)
6. 应用层 Query：{Module}Query (接口) + {Module}QueryImpl (readOnly)
7. 接口层 Facade：{Module}Facade (接口) + {Module}FacadeImpl (@NameJoin)
8. 接口层 REST：{Module}Rest (@Tag + 完整 Swagger 注解)
```

---

## 18. 常见问题与修正指南

> 本节列出了代码审查中发现的常见问题和相应的修正方案，开发时请注意避免。

### 18.1 依赖注入使用 @Resource 替代 @Autowired

**问题描述**：Cmd 实现类中使用 `@Autowired(required = false)` 注入依赖。

```java
// 反例
@Autowired(required = false)
private ArtifactRepo artifactRepo;
```

```java
// 正例
@Resource
private ArtifactRepo artifactRepo;
```

**说明**：`@Resource` 是 Jakarta 标准注解（`jakarta.annotation.Resource`），按名称匹配注入，语义更明确。`@Autowired` 是 Spring 特有注解，`required = false` 可能隐藏注入失败问题。

### 18.2 资源存在性检查使用 xxxQuery.findAndCheck()

**问题描述**：在 Cmd 实现类中直接调用 `repo.findById(id).orElseThrow(...)` 进行资源存在性校验。

```java
// 反例
@Override
@Transactional(rollbackFor = Exception.class)
public void markLatest(Long id) {
  new BizTemplate<Void>() {
    Artifact existing;

    @Override
    protected void checkParams() {
      existing = artifactRepo.findById(id)
          .orElseThrow(() -> ResourceNotFound.of(id, "Artifact"));
    }

    @Override
    protected Void process() {
      // ... 业务逻辑
      return null;
    }
  }.execute();
}
```

```java
// 正例
@Override
@Transactional(rollbackFor = Exception.class)
public void markLatest(Long id) {
  new BizTemplate<Void>() {
    Artifact existing;

    @Override
    protected void checkParams() {
      // 获取并验证是否存在
      existing = artifactQuery.findAndCheck(id);
    }

    @Override
    protected Void process() {
      // ... 业务逻辑
      return null;
    }
  }.execute();
}
```

**说明**：`xxxQuery.findAndCheck()` 方法已封装了资源存在性校验和异常抛出逻辑，遵循 CQRS 模式中查询职责的分离原则，避免在 Cmd 层重复编写校验代码。

### 18.3 继承 TenantAuditingEntity 替代手动审计字段

**问题描述**：实体类继承 `TenantEntity` 并手动定义 `createdBy`、`createdDate`、`modifiedBy`、`modifiedDate` 四个审计字段。

```java
// 反例
public class Artifact extends TenantEntity<Artifact, Long> {
  // ... 业务字段

  @Column(name = "created_by")
  private Long createdBy;

  @Column(name = "created_date", nullable = false, updatable = false)
  private LocalDateTime createdDate;

  @Column(name = "modified_by")
  private Long modifiedBy;

  @Column(name = "modified_date")
  private LocalDateTime modifiedDate;
}
```

```java
// 正例
public class Artifact extends TenantAuditingEntity<Artifact, Long> {
  // ... 业务字段
  // 无需手动定义审计字段，框架内部会自动设置
}
```

**说明**：当实体需要 `createdBy`、`createdDate`、`modifiedBy`、`modifiedDate` 四个审计字段时，应继承 `TenantAuditingEntity`。框架会自动设置这些字段的值，无需在业务代码中手动赋值。

### 18.4 实体类不添加 jakarta validation 注解

**问题描述**：在领域实体类上添加了 `jakarta.validation` 注解（如 `@NotBlank`、`@NotNull`、`@Size` 等）。

```java
// 反例
@Setter
@Getter
@Accessors(chain = true)
public class Vulnerability extends TenantEntity<Vulnerability, String> {

  @NotBlank
  @Column(name = "scan_task_id", nullable = false)
  private String scanTaskId;

  @NotNull
  @Enumerated(EnumType.STRING)
  @Column(nullable = false, length = 20)
  private VulnerabilitySeverity severity;
}
```

```java
// 正例
@Setter
@Getter
@Accessors(chain = true)
public class Vulnerability extends TenantEntity<Vulnerability, String> {

  @Column(name = "scan_task_id", nullable = false)
  private String scanTaskId;

  @Enumerated(EnumType.STRING)
  @Column(nullable = false, length = 20)
  private VulnerabilitySeverity severity;
}
```

**说明**：参数校验应在 DTO 层（接口层）完成，领域实体类不应包含 `jakarta.validation` 注解。实体类通过 JPA 的 `@Column(nullable = false)` 等约束保证数据库层面的完整性，而业务输入校验由 DTO 的 `@Valid` + 校验注解负责。

---

*文档版本: 2.0 | 最后更新: 2026-03 | 基于 AngusRepo v2.0.0 实际代码提炼*
