# AngusRepo 后端未完成业务与不确定项分析

> **生成时间**：2026-03-16  
> **分析范围**：`service/core/src/main/java/cloud/xcan/angus/core/repo/` 全部后端业务代码  
> **参考规范**：`docs/api-design/00-接口开发规范-完整版.md`

---

## 📑 目录

1. [未完成业务汇总](#1-未完成业务汇总)
2. [TODO/FIXME 标记详情](#2-todofixme-标记详情)
3. [桩实现（Stub）方法详情](#3-桩实现stub方法详情)
4. [不确定如何处理的业务项](#4-不确定如何处理的业务项)
5. [BizConstant 常量缺失项](#5-bizconstant-常量缺失项)
6. [规范合规修复记录](#6-规范合规修复记录)

---

## 1. 未完成业务汇总

| 序号 | 模块 | 文件 | 方法/位置 | 严重程度 | 状态 | 说明 |
|------|------|------|-----------|----------|------|------|
| 1 | 趋势分析 | TrendAnalyticsQueryImpl | getTrendingArtifacts() 等 6 个方法 | 🔴 高 | 未实现 | 全部返回空列表，无实际数据聚合 |
| 2 | 仪表盘 | DashboardQueryImpl | getTopRepositories() 等 3 个方法 | 🔴 高 | 未实现 | 返回空列表，仪表盘无数据 |
| 3 | 仪表盘 | DashboardQueryImpl | getOverview() | 🔴 高 | 桩实现 | 所有统计值硬编码为 0 |
| 4 | 分析统计 | AnalyticsQueryImpl | getRepositoryComparison() 等 2 个方法 | 🔴 高 | 未实现 | 返回空列表 |
| 5 | 制品管理 | ArtifactFacadeImpl | download() | 🔴 高 | 未实现 | 文件下载未实现，响应体为空 |
| 6 | 分析统计 | AnalyticsFacadeImpl | exportReport() | 🟡 中 | 桩实现 | 返回固定字符串，无实际报告生成 |
| 7 | 系统设置 | SystemSettingsFacadeImpl | testConnection() | 🟡 中 | 桩实现 | 始终返回成功，未实际测试连接 |
| 8 | 系统设置 | SystemSettingsFacadeImpl | getSystemStatus() | 🟡 中 | 部分实现 | 数据库/搜索状态硬编码为 "UP" |
| 9 | 团队管理 | TeamMemberFacadeImpl | invite() | 🟡 中 | 硬编码 | 使用硬编码 userId=1L 代替安全上下文 |
| 10 | 安全扫描 | DefaultSecurityScanIntegration | triggerScanAfterUpload() | 🟡 中 | 桩实现 | 仅日志输出，无实际漏洞扫描 |
| 11 | 活动日志 | ActivityLogFacadeImpl | export() | 🟢 低 | 部分实现 | Excel 导出未实现，回退为 CSV |
| 12 | 安装配置 | ConfigurableRepoApplication | 许可配额逻辑 | 🟢 低 | 待完善 | 多主应用时配额取最高值逻辑未实现 |

---

## 2. TODO/FIXME 标记详情

### 2.1 TrendAnalyticsQueryImpl（6 个 TODO）

**文件路径**：`application/query/analytics/impl/TrendAnalyticsQueryImpl.java`

```java
// TODO: Implement with actual data aggregation from ArtifactRepo
public List<TrendingArtifactVo> getTrendingArtifacts() { return new ArrayList<>(); }

// TODO: Implement with actual data aggregation from RepoEntityRepo
public List<TrendingRepositoryVo> getTrendingRepositories() { return new ArrayList<>(); }

// TODO: Implement with time-series aggregation
public List<TrendDataPointVo> getDownloadTrend(LocalDate start, LocalDate end) { return new ArrayList<>(); }

// TODO: Implement with time-series aggregation
public List<TrendDataPointVo> getUploadTrend(LocalDate start, LocalDate end) { return new ArrayList<>(); }

// TODO: Implement with time-series aggregation
public List<TrendDataPointVo> getStorageTrend(LocalDate start, LocalDate end) { return new ArrayList<>(); }

// TODO: Implement with time-series aggregation
public List<TrendDataPointVo> getUserActivityTrend(LocalDate start, LocalDate end) { return new ArrayList<>(); }

// TODO: Implement with data aggregation from RepoEntityRepo
public List<FormatDistributionVo> getFormatDistribution() { return new ArrayList<>(); }
```

**分析**：趋势分析模块是核心数据可视化功能，需要基于 `ArtifactRepo`、`RepoEntityRepo` 和活动日志表进行时间序列聚合查询。建议实现方案：
- 热门制品/仓库：基于下载量聚合 `GROUP BY artifact_id/repo_id ORDER BY count DESC LIMIT N`
- 趋势数据：按天/周/月聚合 `GROUP BY DATE(created_date)` 生成时间点数据
- 需新增仓储查询方法或使用 `@Query` 自定义聚合 SQL

### 2.2 AnalyticsQueryImpl（2 个 TODO）

**文件路径**：`application/query/analytics/impl/AnalyticsQueryImpl.java`

```java
// TODO: Implement with actual data aggregation
public List<RepositoryComparisonVo> getRepositoryComparison(...) { return new ArrayList<>(); }

// TODO: Implement with actual data aggregation
public List<FormatUsageVo> getFormatUsage(...) { return new ArrayList<>(); }
```

**分析**：
- 仓库对比：需聚合各仓库的制品数、下载量、存储大小等维度数据
- 格式使用统计：需按包格式（Maven/Docker/NPM/PyPI 等）聚合使用数据

### 2.3 ArtifactFacadeImpl（1 个 TODO）

**文件路径**：`interfaces/artifact/facade/internal/ArtifactFacadeImpl.java`

```java
// TODO: Read artifact file from blob storage and write to response.getOutputStream()
public void download(Long id, HttpServletResponse response) {
    Artifact artifact = artifactQuery.findByIdAndCheck(id);
    // ... 设置 Response Header 但未写入文件内容
}
```

**分析**：文件下载是制品仓库核心功能。需要：
1. 从 Blob 存储（本地文件系统/S3/MinIO）读取制品文件
2. 设置 Content-Type、Content-Disposition 等 Header
3. 流式写入 `response.getOutputStream()`
4. 记录下载次数（更新 `downloadCount`）

### 2.4 SystemSettingsFacadeImpl（1 个 TODO）

**文件路径**：`interfaces/system/facade/internal/SystemSettingsFacadeImpl.java`

```java
// TODO 根据dto.getType()实现具体的连接测试逻辑（LDAP/SAML/S3/SLACK/SMTP）
public ConnectionTestResultVo testConnection(ConnectionTestDto dto) {
    result.setSuccess(true);      // 始终返回成功
    result.setMessage("连接测试成功");
}
```

**分析**：连接测试需要根据 `dto.getType()` 分别实现：
- **LDAP**：使用 `LdapTemplate` 测试 bind 操作
- **SAML**：验证 IdP metadata URL 可达
- **S3**：使用 AWS SDK `headBucket()` 测试存储桶访问
- **SLACK**：通过 Webhook URL 发送测试消息
- **SMTP**：使用 `JavaMailSender` 测试 SMTP 连接

### 2.5 TeamMemberFacadeImpl（1 个 TODO）

**文件路径**：`interfaces/team/facade/internal/TeamMemberFacadeImpl.java`

```java
// TODO: Replace with PrincipalContext.getUserId() when security context is available
Long invitedBy = 1L;  // 硬编码
```

**分析**：当 Spring Security 上下文可用后，应替换为 `PrincipalContext.getUserId()`，否则邀请记录中的邀请人 ID 始终为 1。

### 2.6 DefaultSecurityScanIntegration（1 个 TODO）

**文件路径**：`infra/store/DefaultSecurityScanIntegration.java`

```java
// TODO: Integrate with external vulnerability scanning service
log.info("Security scan triggered for artifact={}, format={}", artifactId, format);
```

**分析**：安全扫描集成需要对接外部漏洞扫描服务（如 Trivy、Clair、Snyk），实现上传后自动触发扫描并回写扫描结果到 `ScanTask`。

### 2.7 ConfigurableRepoApplication（1 个 TODO）

**文件路径**：`infra/installation/ConfigurableRepoApplication.java`

```java
// Update license quota, TODO When multiple main applications are used,
// the highest quota value should be update
```

**分析**：多主应用场景下的许可配额取最高值逻辑需要补充。

---

## 3. 桩实现（Stub）方法详情

### 3.1 DashboardQueryImpl — 仪表盘数据全部为桩

**文件路径**：`application/query/dashboard/impl/DashboardQueryImpl.java`

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `getOverview()` | 所有字段硬编码为 0 | 需查询 ArtifactRepo/RepoEntityRepo/UserProfileRepo 获取实际计数 |
| `getTopRepositories()` | 空列表 `new ArrayList<>()` | 需按制品数或下载量排序查询仓库 |
| `getRecentActivity()` | 空列表 `new ArrayList<>()` | 需查询最近活动记录 |
| `getStorageDistribution()` | 空列表 `new ArrayList<>()` | 需按格式/仓库聚合存储用量 |

### 3.2 AnalyticsFacadeImpl.exportReport() — 报告导出未实现

```java
public String exportReport(AnalyticsExportDto dto) {
    // TODO: Implement async report generation
    return "Report generation started";
}
```

**分析**：应实现异步报告生成流程，包含：
1. 创建导出任务记录
2. 异步线程生成 PDF/CSV/Excel 报告
3. 上传到文件存储
4. 返回下载链接

### 3.3 SystemSettingsFacadeImpl.getSystemStatus() — 健康检查不完整

```java
status.setDatabaseStatus("UP");  // 硬编码，未实际检测
status.setSearchStatus("UP");    // 硬编码，未实际检测
```

**分析**：应使用 Spring Boot Actuator 或自定义 HealthIndicator 检测实际服务状态。

### 3.4 ActivityLogFacadeImpl.export() — Excel 格式降级为 CSV

```java
if ("csv".equals(format)) {
    exportCsv(logs, response);
} else {
    // Excel格式需要额外的库支持，这里先返回CSV
    exportCsv(logs, response);  // 降级处理
}
```

**分析**：需引入 Apache POI 或 EasyExcel 实现 Excel 导出。

---

## 4. 不确定如何处理的业务项

### 4.1 ActivityLogCmdImpl.create() — null 输入处理策略

**文件路径**：`application/cmd/activitylog/impl/ActivityLogCmdImpl.java`

```java
public ActivityLog create(ActivityLog activityLog) {
    if (Objects.isNull(activityLog)) {
        return null;  // 返回 null 而非抛异常
    }
    // ...
}
```

**不确定点**：按照规范，应该抛出 `ProtocolException` 或 `IllegalArgumentException`，但此方法可能被内部批量调用，返回 null 可能是有意设计。  
**建议**：确认调用方是否处理了 null 返回值。若为外部门面层调用，应改为抛异常；若为内部批量处理，保持 null 返回但在调用方做防御。

### 4.2 ActivityQueryImpl.findSummaryByTarget() — null 返回策略

**文件路径**：`application/query/activity/impl/ActivityQueryImpl.java`

```java
if (isEmpty(activities)) {
    return null;  // 查询无结果时返回 null
}
```

**不确定点**：按照规范，查询方法在无结果时应返回空集合而非 null。但 `findSummaryByTarget` 返回的是单个 VO 而非列表。  
**建议**：改为返回空的 VO 对象或使用 `Optional<T>` 包装。

### 4.3 PyPIFormatHandler.extractPythonVersionFromWheel() — null 返回处理

**文件路径**：`application/format/PyPIFormatHandler.java`

```java
private String extractPythonVersionFromWheel(String fileName) {
    String[] parts = fileName.replace(".whl", "").split("-");
    if (parts.length >= 5) {
        return parts[parts.length - 3];
    }
    return null;  // 无法解析时返回 null
}
```

**不确定点**：如果 wheel 文件名格式不合规，返回 null 可能导致调用方 NPE。  
**建议**：调用方应做 null 检查，或此方法返回默认值 `"unknown"`。

### 4.4 ArtifactRest.removeStar() — DELETE 方法有返回值

**文件路径**：`interfaces/artifact/ArtifactRest.java`

```java
@DeleteMapping("/{id}/star")
public ApiLocaleResult<?> removeStar(@PathVariable Long id) {
    // ...
    return ApiLocaleResult.success();
}
```

**不确定点**：规范要求 DELETE 操作返回 `void` + HTTP 204。但「取消收藏」是语义上的 DELETE（删除收藏关系），而非资源删除。  
**建议**：两种方案均可接受：
- 方案 A：改为 `void` + 204（严格遵守规范）
- 方案 B：保持返回 200 + 空数据（语义更清晰，表示操作成功）

### 4.5 @Size 大值（500/1000/2000/4000）缺少 BizConstant

规范要求所有 `@Size(max=)` 使用 `BizConstant` 常量，但以下值没有对应常量：

| 当前值 | 涉及文件数 | 使用场景 | 建议新增常量 |
|--------|-----------|---------|-------------|
| 500 | 5 | description/url/path 字段 | `MAX_LONG_DESC_LENGTH = 500` |
| 1000 | 4 | 规则表达式/大段描述 | `MAX_EXPRESSION_LENGTH = 1000` |
| 2000 | 8 | 详细描述/webhook url | `MAX_CONTENT_LENGTH = 2000` |
| 4000 | 1 | 大文本元数据 | `MAX_METADATA_LENGTH = 4000` |

**建议**：在 `BizConstant` 中新增上述常量，然后替换 DTO 中的硬编码值。

### 4.6 格式协议层（Protocol）REST 控制器的规范适用性

10 个格式协议 REST 控制器（MavenProtocolRest、DockerProtocolRest 等）实现的是原生包管理器协议（如 Maven Deploy、Docker Registry API），不是标准的 CRUD REST API。

**不确定点**：
- 这些协议端点的 URL 路径遵循各包管理器规范（如 `/v2/` for Docker），不遵循 `/api/v1/` 规范
- HTTP 方法和状态码遵循各协议规范，不遵循通用 REST 规范
- 响应格式不使用 `ApiLocaleResult<T>` 包装

**建议**：这些协议层控制器应被豁免于通用 REST 接口规范，但仍需添加 `@Parameter` 注解以支持 API 文档生成。

---

## 5. BizConstant 常量缺失项

以下 DTO 文件中的 `@Size` 注解仍使用硬编码数字，需要在 `BizConstant` 中定义新常量后替换：

| 文件 | 字段 | 当前值 | 建议常量 |
|------|------|--------|---------|
| AccessRuleCreateDto | description | 2000 | MAX_CONTENT_LENGTH |
| AccessRuleUpdateDto | description | 2000 | MAX_CONTENT_LENGTH |
| AccessTokenCreateDto | description | 2000 | MAX_CONTENT_LENGTH |
| ArtifactCreateDto | description | 2000 | MAX_CONTENT_LENGTH |
| ArtifactUpdateDto | description | 2000 | MAX_CONTENT_LENGTH |
| CleanupPolicyCreateDto | expression | 1000 | MAX_EXPRESSION_LENGTH |
| CleanupPolicyUpdateDto | expression | 1000 | MAX_EXPRESSION_LENGTH |
| FormatArtifactUploadDto | filePath | 500 | MAX_LONG_DESC_LENGTH |
| NotificationCreateDto | content | 500 | MAX_LONG_DESC_LENGTH |
| NotificationUpdateDto | content | 500 | MAX_LONG_DESC_LENGTH |
| WebhookCreateDto | url | 2000 | MAX_CONTENT_LENGTH |
| WebhookUpdateDto | url | 2000 | MAX_CONTENT_LENGTH |
| RepositoryCreateDto | description | 2000 | MAX_CONTENT_LENGTH |
| RepositoryUpdateDto | description | 2000 | MAX_CONTENT_LENGTH |
| ScanPolicyCreateDto | rules | 1000 | MAX_EXPRESSION_LENGTH |
| ScanPolicyUpdateDto | rules | 1000 | MAX_EXPRESSION_LENGTH |
| GeneralSettingsUpdateDto | description | 500 | MAX_LONG_DESC_LENGTH |
| UploadCompleteDto | metadata | 4000 | MAX_METADATA_LENGTH |
| UploadCompleteDto | description | 2000 | MAX_CONTENT_LENGTH |
| UploadCompleteDto | checksum | 500 | MAX_LONG_DESC_LENGTH |
| UploadFileInfoDto | filePath | 500 | MAX_LONG_DESC_LENGTH |
| UploadFileInfoDto | checksum | 1000 | MAX_EXPRESSION_LENGTH |
| UploadTaskCreateDto | description | 500 | MAX_LONG_DESC_LENGTH |
| UploadTaskCreateDto | metadata | 1000 | MAX_EXPRESSION_LENGTH |

---

## 6. 规范合规修复记录

以下为本次代码审查中已修复的不合规项：

### 6.1 已修复项

| 序号 | 修复内容 | 影响范围 | 文件数 |
|------|---------|---------|--------|
| 1 | 所有 QueryImpl 类添加 `@Transactional(readOnly = true)` | 事务管理规范 §13 | 18 |
| 2 | AnalyticsRest/TrendAnalyticsRest 添加 `@Valid` 到 `@ParameterObject` 参数 | 参数校验规范 §7 | 2 |
| 3 | 所有 REST 控制器的 `@PathVariable` 添加 `@Parameter` 注解 | API 文档规范 §14 | 26 |
| 4 | AnalyticsRest/TrendAnalyticsRest 所有方法添加 `@ResponseStatus` | HTTP 状态码规范 §5 | 2 |
| 5 | 122 个 DTO/VO 类添加 `implements Serializable` | DTO/VO 规范 §16.5 | 122 |
| 6 | 21 个 DTO 中的 `@Size(max = 100/200/255)` 替换为 BizConstant 常量 | 常量使用规范 §2.4 | 21 |
| 7 | ActivityFindDto 添加 `@Schema` 类注解 | API 文档规范 §14 | 1 |
| 8 | ActivityDetailVo 添加 `@Schema` 类注解和所有字段 `@Schema` | API 文档规范 §14 | 1 |

### 6.2 合规项（无需修改）

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 所有 REST 控制器有 `@Tag`、`@Validated`、`@RestController` | ✅ 合规 | 所有 21 个控制器 |
| 所有 REST 方法有 `@Operation(operationId=...)` | ✅ 合规 | 所有方法 |
| 所有 REST 方法有 `@ApiResponses` | ✅ 合规 | 所有方法 |
| POST 方法返回 201 + `@ResponseStatus(CREATED)` | ✅ 合规 | 所有 POST |
| DELETE 方法返回 204 + void | ✅ 合规 | 所有标准 DELETE |
| 控制器仅调用 Facade | ✅ 合规 | 无越级调用 |
| 使用 `@Resource` 注入 | ✅ 合规 | 无 `@Autowired` |
| Facade 无 `@Transactional` | ✅ 合规 | 所有 18 个实现 |
| Facade 无直接 Repo 调用 | ✅ 合规 | 通过 Cmd/Query |
| FindDto 继承 PageQuery 并重写 getDefaultOrderBy() | ✅ 合规 | 所有 13 个 FindDto |
| Cmd 方法有 `@Transactional(rollbackFor = Exception.class)` | ✅ 合规 | 所有写操作 |
| 使用 BizTemplate 包装业务 | ✅ 合规 | 大部分 Cmd/Query 方法 |

---

*文档版本: 1.0 | 生成时间: 2026-03-16 | 基于规范 v2.0 代码审查*
